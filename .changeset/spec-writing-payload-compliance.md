---
"training-app": patch
---

Extend spec-writing skill with stack-specific compliance reviews. Split the Medusa-only compliance matrix into a universal dispatcher plus separate `compliance-review-medusa.md` and `compliance-review-payload.md` reference files. Update `SKILL.md` and `spec-template.md` to point to the correct file per project stack.
